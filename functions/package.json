{
  "name": "regicide-functions",
  "description": "Firebase Cloud Functions for Regicide online multiplayer",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "npx shadow-cljs release functions",
    "watch": "npx shadow-cljs watch functions",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.0"
  }
}
